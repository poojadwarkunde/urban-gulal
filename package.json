{
  "name": "urban-gulal",
  "version": "1.0.0",
  "description": "Urban Gulal - Decorative & Gift Items Shop",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "echo 'Run backend and frontend separately'",
    "build": "npm config set registry https://registry.npmjs.org/ && cd frontend && npm install --registry https://registry.npmjs.org/ && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/",
    "start": "cd backend && node server.js",
    "postinstall": "npm config set registry https://registry.npmjs.org/ && cd backend && npm install --registry https://registry.npmjs.org/"
  }
}
